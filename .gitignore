# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# typescript
*.tsbuildinfo
next-env.d.ts

# Note: lib/articleIndex.generated.ts is committed (regenerated on every build by
# the prebuild script) so the per-article OG import resolves even if a host's
# build command skips npm lifecycle hooks.

# vercel
.vercel
